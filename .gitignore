__pycache__/
*.pyc

# Raw uCT scans (.VOX), experiment folders and derived results live in the
# working tree but are NOT part of the repo — only the pipeline code + atlas.npz
# are tracked. Ignore all top-level folders except uct/ (and the atlas source).
/*/
!/uct/
/uct/atlas_segments/
